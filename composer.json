{
    "name": "matesofmate/phpunit-extension",
    "description": "PHPUnit extension for symfony/ai-mate providing token-optimized test execution tools",
    "type": "symfony-ai-mate",
    "license": "MIT",
    "keywords": [
        "phpunit",
        "testing",
        "ai-mate",
        "mcp",
        "test-automation",
        "toon",
        "symfony",
        "ai"
    ],
    "authors": [
        {
            "name": "Johannes Wachter",
            "email": "johannes@sulu.io"
        }
    ],
    "require": {
        "php": ">=8.2",
        "matesofmate/common": "^0.3",
        "symfony/ai-mate": "^0.8",
        "symfony/finder": "^5.4|^6.4|^7.3|^8.0",
        "symfony/process": "^5.4|^6.4|^7.3|^8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "friendsofphp/php-cs-fixer": "^3.0",
        "phpstan/phpstan": "^2.0",
        "phpstan/phpstan-phpunit": "^2.0",
        "rector/rector": "^2.0"
    },
    "suggest": {
        "helgesverre/toon": "Install for TOON-encoded MCP responses; JSON remains the fallback when it is not available"
    },
    "autoload": {
        "psr-4": {
            "MatesOfMate\\PHPUnitExtension\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "MatesOfMate\\PHPUnitExtension\\Tests\\": "tests/"
        }
    },
    "extra": {
        "ai-mate": {
            "scan-dirs": [
                "src/Capability"
            ],
            "includes": [
                "config/config.php"
            ],
            "instructions": "INSTRUCTIONS.md"
        },
        "branch-alias": {
            "dev-main": "0.3.x-dev"
        }
    },
    "scripts": {
        "test": "vendor/bin/phpunit",
        "lint": [
            "@composer validate --strict",
            "vendor/bin/rector process --dry-run",
            "vendor/bin/php-cs-fixer fix --dry-run --diff",
            "vendor/bin/phpstan analyse"
        ],
        "fix": [
            "vendor/bin/rector process",
            "vendor/bin/php-cs-fixer fix"
        ]
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}
